@model IEnumerable<ProyectoASPTrimestre3.Models.usuariorol>

@{
    ViewBag.Title = "Usuario Rol";
}

<h2>Usuario Rol</h2>

<p>
    @Html.ActionLink("Crear Rol", "Create")
</p>
<table class="table">
    <tr>
        <th>
            Descripcion Rol:
        </th>
        <th>
            Nombre Usuario:
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @ProyectoASPTrimestre3.Controllers.UsuarioRolController.nombreRol(item.idRol)
            </td>
            <td>
                @ProyectoASPTrimestre3.Controllers.UsuarioRolController.nombreUsuario(item.idUsuario)
            </td>
            <td>
                @Html.ActionLink("Editar", "Edit", new { id = item.id }) |
                @Html.ActionLink("Detalles", "Details", new { id = item.id }) |
                @Html.ActionLink("Eliminar", "Delete", new { id = item.id }, new { @class = "linkDelete" })
            </td>
        </tr>
    }

</table>

@section scripts{
    <script>
        $(".linkDelete").click(function (event){
            if (!confirm('¿Está seguro de eliminar?')) {
                event.preventDefault();
            }
        })
    </script>

}