@model ProyectoASPTrimestre3.Models.ModeloIndex

@{
    ViewBag.Title = "Usuario Rol";
}

<h2>Usuario Rol</h2>

<p>
    @Html.ActionLink("Crear Rol", "Create")
</p>
<table class="table">
    <tr>
        <th>
            Descripcion Rol:
        </th>
        <th>
            Nombre Usuario:
        </th>
        <th></th>
    </tr>

    @foreach (var usuariorol in Model.Usuarioroles)
    {
        <tr>
            <td>
                @ProyectoASPTrimestre3.Controllers.UsuarioRolController.nombreRol(usuariorol.idRol)
            </td>
            <td>
                @ProyectoASPTrimestre3.Controllers.UsuarioRolController.nombreUsuario(usuariorol.idUsuario)
            </td>
            <td>
                @Html.ActionLink("Editar", "Edit", new { id = usuariorol.id }) |
                @Html.ActionLink("Detalles", "Details", new { id = usuariorol.id }) |
                @Html.ActionLink("Eliminar", "Delete", new { id = usuariorol.id }, new { @class = "linkDelete" })
            </td>
        </tr>
    }

</table>

@section scripts{
    <script>
        $(".linkDelete").click(function (event) {
            if (!confirm('¿Está seguro de eliminar?')) {
                event.preventDefault();
            }
        })
    </script>

}

@{ Html.RenderPartial("Paginador", Model); }
